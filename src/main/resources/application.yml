spring:
  profiles:
    active:
      - local

---

spring:
  profiles: local
  datasource:
    url: jdbc:mysql://localhost:3306/tcrmd00
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
    
server:
  port: 8081
  